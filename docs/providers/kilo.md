# Kilo

Credential: API key.

Kilo is included as a coding-plan provider for local tracking. Save the API key only when you want TokenManager to hold a Keychain reference for future live refresh support.

TokenManager does not proxy or upload the key.
