# OpenCode

Credential: Cookies + Go local session.

OpenCode is tracked as a local/session provider. TokenManager records the account locally and does not upload browser cookies or session material.

Live quota refresh is disabled until a stable, documented local API contract is available.
