# z.ai

Credential: API key.

z.ai is represented as a coding-plan provider. TokenManager stores the API key in macOS Keychain and keeps the provider ready for a live adapter once the public remaining-quota endpoint is stable.

Common coding-plan base:

```text
https://api.z.ai/api/coding/paas/v4
```

Do not paste real keys into issues, docs, or screenshots.
