# Kiro

Credential: CLI.

Kiro is represented as a CLI/local-session provider. TokenManager can keep the provider in the catalog and open its docs/dashboard while a live local adapter is investigated.

Do not store CLI session files in this repository.
