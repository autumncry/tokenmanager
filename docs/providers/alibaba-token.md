# Alibaba Token

Credential: Cookies.

This provider is separate from the standard Alibaba API-key entry because some token/coding-plan quota data is exposed through browser-session flows rather than a normal Bearer API.

TokenManager stores only local references and never sends cookie data to a TokenManager service.
