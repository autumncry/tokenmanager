# Gemini

Credential: OAuth session or API key.

Use this entry for Google AI Studio/Gemini usage tracking. TokenManager keeps account metadata local and links to AI Studio usage while live quota refresh is pending.

Saved keys are stored in macOS Keychain only.
