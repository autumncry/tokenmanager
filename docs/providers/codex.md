# Codex

Credential: OAuth session.

Codex plan data is represented as a first-class provider so it can be tracked alongside API-account providers. A stable local quota endpoint is not documented yet, so TokenManager keeps this entry catalog-ready and stores only local references/notes for now.

Open the Codex dashboard from the provider page, then record the account locally in TokenManager. No credential is uploaded to a TokenManager server.
